# Azure Data Factory Aritfacts

- `ForEachInput.json`: your existing parameterization for iteration.
- (Optional) Export your Copy pipeline JSON here, e.g., `Ingest_Olist_To_Bronze.json`.

## Import instructions
In ADF Studio → Manage → ARM template → Import/Export, or use the "Export ARM template" on your existing pipeline and drop the JSON files into this folder for reference.